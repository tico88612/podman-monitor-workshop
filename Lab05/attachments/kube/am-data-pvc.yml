apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    volume.podman.io/driver: local
  name: am-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1G
